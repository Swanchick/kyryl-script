let variable: int = null; -- Може бути не тільки null

function foo(bar: int) {
    bar?; -- зробити return замість написання його власноруч.
    -- Або
    bar? {
        -- Зробити щось до повернення
    };
}

foo(variable);