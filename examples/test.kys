function sum(a: [int]): int {
    let out = 0;

    for i in a {
        out += i;
    }
    
    return out;
}



function main() {
    -- let a = [[ [10, 20], [30, 5] ], [[15, 25], [20, 5]]];
    -- println(a);

    -- a[1][0][4] = 50;
    -- println(a);

    -- let b = [1, 2, 3, 4, 5, 6, 7, 8];
    -- println(b);

    -- b[2] = 50;
    -- println(b);

    -- let name = "Name";
    -- name[1] = "e";
    -- println(name);

    let a = "Hello World";

    for i in a {
        println(i);
    }

    let b = [40, 2, 4, 7, 4, 34, 68, 12];

    println(sum(b));

}