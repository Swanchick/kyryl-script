---
--- This code has been written completely by scratch
--- by Swanchick!
--- Hell yeah
---

function recursion_test(iter: int) {
    unprint(iter);

    if iter ~= 10 {
        recursion_test(iter + 1);
    }
}

function main() {
    recursion_test(0);
}

